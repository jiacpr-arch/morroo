# แผน: ใช้ "เกมเคส" เป็นหัวกรวยดึงลูกค้าผ่านการยิงแอด

> ต่อยอดจาก `docs/ads-plan-2026H2.md` และ `docs/ads-campaign-setup-2026H2.md`
> เอกสารนี้ = กลยุทธ์ + สถานะงานฝั่งระบบ · เขียน 2026-07-24 · แก้ทิศทาง 2026-07-25

> ### ⚠️ เปลี่ยนทิศทาง 2026-07-25 — เลิกเก็บ lead
>
> เจ้าของตัดสินใจว่า **"ต้องการแค่ให้ลูกค้าเข้าในเว็บแล้วตัดสินใจดีกว่า การเก็บหลีดไม่ค่อยได้ผลแล้วไม่มีพนักงานดูแลต่อ"**
>
> ข้อมูลที่รองรับ: lead จากแชท 40 รายค้างที่ stage `new` ทั้งหมด (23 fb_messenger + 17 line_oa) ไม่มีอีเมล/ชื่อ/consent สักราย 38 ใน 40 เก่ากว่า 24 ชม. (เก่าสุด 10 พ.ค.) เพราะ cron `lead-followup` จับเฉพาะ `code_issued` — การเก็บอีเมลจึงเป็น friction ที่ไม่ได้แปลงเป็นอะไรเลย
>
> **ผลที่ตามมาทั้งหมด:**
> - ฟอร์มขออีเมลท้ายเกมถอดออกแล้ว → `components/sim/DebriefBrowseCta.tsx` (ลิงก์เข้าเนื้อหาแทน)
> - event `casegame_lead` **ยกเลิก** · ตัวชี้ขาดใหม่คือ `casegame_cta_click` (ดู §4)
> - โฆษณา 4 ตัวที่พาไป `/register` ปิดแล้ว แทนด้วยชุดใหม่ที่พาเข้าหน้าเนื้อหา (ดู §3.5)
> - แคมเปญ `[MR]_Traffic_FreeTrial` เปลี่ยนชื่อเป็น `[MR]_Traffic_Browse` และเปลี่ยน optimization จาก `OFFSITE_CONVERSIONS` → `LANDING_PAGE_VIEWS`
> - **ต้องเปิด `[MR]_Retarget`** — เมื่อไม่เก็บอีเมล คนที่เข้ามาแล้วยังไม่สมัครจะติดต่อกลับไม่ได้เลย เหลือทางเดียวคือ retarget ผ่าน Custom Audience

---

## 1. ปัญหาที่กำลังแก้

`docs/ads-plan-2026H2.md` สรุปไว้ว่าเราใช้งบแอดไป ~฿12,000 แลกผู้ใช้ 51 คน / รายได้ ฿3,776 (ROAS ติดลบ) และระบุสาเหตุรากไว้ที่ข้อ 3:

> **Optimize ผิดจังหวะ** — Meta ต้องการ ~50 conversion/สัปดาห์ แต่เราได้สมัครรวมแค่ ~5/สัปดาห์ → อัลกอริทึม optimize ไม่ได้

แคมเปญ `MorRoo - Signup` เลยได้ **428 คลิก → สมัครจริง 1 คน** (CPR ฿1,221)

**เกมเคสแก้ตรงจุดนี้** เพราะ "เริ่มเล่นเกม" เกิดบ่อยกว่า "สมัครสมาชิก" หลายเท่า — เป็น conversion event ตัวแรกที่มีปริมาณพอให้ Meta ออกจาก learning phase ได้จริง

เหตุผลประกอบอีกสองข้อ:
- **เล่นได้โดยไม่ต้องล็อกอิน** — friction ต่ำกว่า `/register` มาก และเป็นข้อได้เปรียบที่ห้ามทิ้ง
- **คนเล่นจบเกม = warm pool คุณภาพสูงสุดที่เรามี** → ป้อนแคมเปญ C `[MR]_Retarget` ที่ตอนนี้ paused ใช้งบ ฿0

---

## 2. สิ่งที่ทำไปแล้ว (ฝั่งระบบ)

ก่อนหน้านี้ตัวเกม **ไม่ยิง analytics event เลยแม้แต่ตัวเดียว** — ยิงแอดไปก็วัดอะไรไม่ได้ ตอนนี้อุดแล้ว

### 2.1 Event ที่ยิงจากตัวเกม

ใช้ชื่อ event เดียวครอบทั้ง Code Blue (ACLS) และเกมเคส แล้วแยกด้วย prop `category`
(`components/sim/SimRunner.tsx` เรียก payload builder ใน `lib/sim/track.ts`)

| Event | ยิงเมื่อ | ใช้ดูอะไร |
|---|---|---|
| `casegame_start` | กด "รับเคส" / "เล่นอีกครั้ง" | ปริมาณหัวกรวย · `is_replay` แยกเล่นซ้ำ |
| `casegame_first_decision` | ตัดสินใจข้อแรก | สัญญาณ "ไม่ใช่คนเปิดผ่าน" |
| `casegame_complete` | จบเคส | เกรด/แพ้ชนะ/เวลาจริง (`duration_sec`) |
| `casegame_cta_view` | CTA โผล่บนจอ debrief | **ตัวหารของขั้นสุดท้าย** |
| `casegame_cta_click` | คลิก CTA ท้ายเกม | **ตัวชี้ขาดของแคมเปญ** · `target` = `browse_nl` / `browse_longcase` / `browse_exams` / `login` |

> ~~`casegame_lead`~~ ยกเลิกแล้ว 2026-07-25 พร้อมกับการถอดฟอร์มขออีเมล (ดูกล่องเตือนบนหัวเอกสาร) ข้อมูลเก่าของ event นี้มี 0 แถวอยู่แล้ว จึงไม่มีอะไรตกหล่น

ทั้งหมดข้ามเมื่ออยู่ในโหมด `practice` (admin playtest) จะได้ไม่ปนสถิติจริง

> **`run_id` ติดไปกับทุก event ของเกม** (`start` / `first_decision` / `complete` / `cta_view` / `cta_click`) — 1 รอบเล่น = 1 id
> จำเป็นเพราะสองเรื่อง: (1) โหมด autostart ทำให้การ **รีโหลดหน้า** นับเป็น `start` ใหม่ทุกครั้ง แยกจากการกด "เล่นอีกครั้ง" ไม่ออกถ้าไม่มี id, (2) ถ้าไม่มี id การจับคู่ `start` กับ `complete` ทำได้แค่ระดับ session+slug ซึ่งพังทันทีที่คนเล่นเคสเดิมซ้ำ
>
> ```sql
> -- funnel ที่ dedupe การรีโหลดออกแล้ว
> select count(distinct properties->>'run_id') filter (where event_name='casegame_start')     as starts,
>        count(distinct properties->>'run_id') filter (where event_name='casegame_cta_view')  as saw_cta,
>        count(distinct properties->>'run_id') filter (where event_name='casegame_cta_click') as went_browsing
> from analytics_events where created_at > now() - interval '7 days';
> ```

> **ทำไมต้องมี `cta_view`:** CTA โผล่เฉพาะคนที่ **เล่นจบ + ยังไม่ล็อกอิน + ไม่ใช่โหมดซ้อม** ถ้าวัดแต่ `casegame_cta_click` แล้วเห็นเลข 0 จะแยกไม่ออกเลยว่า "ไม่มีใครเล่นถึง", "เห็นแล้วไม่กด" หรือ "CTA พัง" — ซึ่งเป็นสามปัญหาที่แก้คนละทางกันหมด

### 2.2 ส่ง conversion เข้า Meta

`app/api/track/casegame/route.ts` — ยิง **server-side ทางเดียว** ไม่มี browser copy จึงไม่ต้อง dedupe และรอด ad blocker (middleware เคยพบว่า ~97% ของคลิกโฆษณามองไม่เห็นจาก browser pixel)

ใช้ standard event `ViewContent` + convention ของ `content_name` แทนการเพิ่ม custom event เพราะ Meta สร้าง **Custom Conversion** จาก standard event + กติกา `content_name`/`content_type` ได้อยู่แล้ว และใช้เป็น optimization target ได้เต็มรูปแบบ

- `content_type` = `casegame`
- `content_name` = `casegame_start:<slug>` / `casegame_complete:<slug>`
- `event_id` = `casegame:<start|complete>:<runId>` (1 รอบเล่น = 1 id → beacon ยิงซ้ำไม่ถูกนับสอง conversion)

กันสแปม: rate limit ต่อ IP (40/นาที) + ตรวจว่า `slug` เป็นเคสที่มีอยู่จริง — ถ้าไม่กัน ใครก็ยิง conversion ปลอมจนอัลกอริทึมโฆษณาเรียนรู้ผิดได้

### 2.3 CTA ท้ายเกม

เดิมหน้า debrief มีแค่ลิงก์ "เข้าสู่ระบบ เพื่อเก็บ XP" ตัวเล็กๆ — ขอให้คนแปลกหน้าสมัครสมาชิกเพื่อแลก XP ไม่คุ้มในสายตาเขา ทั้งที่จังหวะเพิ่งเล่นจบคือจุด intent สูงสุดของทั้ง funnel

ระหว่างวันที่ 24–25 ก.ค. ตรงนี้เคยเป็นฟอร์มขออีเมลแลกข้อสอบฟรี 10 ข้อ — **ถอดออกแล้ว** ตามการเปลี่ยนทิศทาง (กล่องเตือนบนหัวเอกสาร)

ตอนนี้เป็น `components/sim/DebriefBrowseCta.tsx` — ไม่ขออะไรเลย ยื่นทางเข้าไปดูของจริงในเว็บให้ 2 ทาง เลือกตามหมวดของเคสที่เพิ่งเล่น:

| เล่นเคสหมวด | ปุ่มที่ได้ |
|---|---|
| `longcase` | `/longcase` (ซ้อมตัวเต็มกับ AI Examiner) · `/nl/practice` |
| `acls` / `meq` | `/nl/practice` (ทำข้อสอบจริงฟรี) · `/exams` |

`/nl/practice` เป็นตัวหลักเพราะเปิดปุ๊บได้ข้อสอบทันทีโดยไม่ต้องล็อกอิน ทำฟรี 5 ข้อแล้วค่อยเจอกำแพง (`components/McqPractice.tsx`) — หลักการเดียวกับ `start=1` ของเกม คือ engage ทันที

ยังคงลิงก์ "เข้าสู่ระบบ" ไว้เป็นทางรองสำหรับคนที่มีบัญชีอยู่แล้ว

### 2.4 Attribution

`?start=1` อ่านจาก URL ที่หน้า `/sim/[slug]` และหน้ารวม `/casegame` ส่ง `utm_*` + `start` ต่อไปยังลิงก์เข้าเล่นด้วย — ไม่งั้นเทียบปลายทางโฆษณาสองแบบ (ข้อ 3.2) ไม่ได้เลย

`campaign` / `adSet` เดิมถูกส่งลงไปถึง `SimRunner` เพื่อติดไปกับ lead — ถอดออกแล้วพร้อมฟอร์ม การเทียบ ad ต่อจากนี้ทำจากฝั่ง Meta (LPV/คลิก ต่อ ad) คู่กับ `path` ของ `casegame_*` ใน `analytics_events`

### 2.5 รายงาน

`analytics_events` ไม่มี allowlist ฝั่งเขียน แต่ฝั่งอ่าน hardcode ชื่อ event ไว้ จึงลงทะเบียนเพิ่มที่:
- `app/(morroo)/admin/analytics/page.tsx` — การ์ด "เกมเคส — funnel" (เริ่มเล่น → ตัดสินใจข้อแรก → เล่นจบ → **เห็น CTA** → กดเข้าดูเนื้อหาต่อ)
  ขั้นสุดท้ายหารด้วย **ยอดเห็น CTA** ไม่ใช่ยอดเล่นจบ — ยอดเล่นจบรวมคนที่ล็อกอินอยู่แล้วซึ่งไม่เคยเห็น CTA ทำให้ conversion ต่ำกว่าจริงเสมอ
- `lib/analytics-weekly.ts` — เข้า digest รายสัปดาห์

---

## 3. สถานะการยิงแอด

> อัปเดต 2026-07-25 — ✅ = ทำแล้ว · ⬜ = ยังต้องทำ

### 3.1 ก่อนเปิดงบบาทแรก
- [x] **ยืนยันว่า event ยิงจริงบน production** — `casegame_start` / `casegame_first_decision` / `casegame_complete` มีข้อมูลจริงในตาราง `analytics_events` แล้ว
  ⚠️ ตัวแรกสุดในตารางคือ **25 ก.ค. 2026** — ก่อนหน้านั้น `/sim/<slug>` มี 231 pageviews โดยไม่มี event เลย ข้อมูล funnel ย้อนหลังจึงไม่มี ใช้เทียบ baseline ไม่ได้
- [ ] **ยืนยัน `casegame_cta_view` → `casegame_cta_click` ยิงจริงบน production** — สองตัวนี้ยังไม่เคยมีข้อมูลสักแถว (e2e ครอบแล้วว่ายิงถูกใน browser จริง แต่ยังไม่มีผู้เล่นจริงเดินผ่านครบ)
- [ ] **ยืนยันฝั่ง Meta ด้วย Test Events** — ตั้ง `META_TEST_EVENT_CODE` แล้วเล่น 1 รอบ ต้องเห็น `ViewContent` พร้อม `content_type: casegame`
  ⚠️ ยังไม่ได้ทำ · dataset stats รวมแยกไม่ได้ว่า `ViewContent` ใบไหนมาจากเกม เพราะหน้า `/register` ก็ยิง event เดียวกัน
- [ ] **สร้าง Custom Conversion "เล่นเกมเคสจริง"** บน pixel `966371002896288` จากกติกา `content_name` **ขึ้นต้นด้วย** `casegame_first_decision`
  ⚠️ **ต้องทำในหน้า Events Manager เอง** — MCP ไม่มีเครื่องมือสร้าง Custom Conversion · ตัวนี้คือ optimization target ของทั้งแผน
  ⚠️ **ห้ามใช้ `casegame_start`** — โฆษณายิงเข้าโหมด autostart (`?start=1`) เกมจึงเริ่มเองทุกครั้งที่หน้าโหลด `start` เลยมีค่าเท่ากับ Landing Page View ใช้เป็น optimization target ไม่ได้ · "ตัดสินใจข้อแรก" คือสัญญาณแรกที่พิสูจน์ว่าเล่นจริง และยังมีปริมาณพอให้เรียนรู้ (~70% ของคนที่เข้า)
- [x] **สร้าง Custom Audience** `[MR] คนเล่นเกมเคส 180 วัน` = `52588558510197`
  กติกาใช้ URL (`url i_contains "/sim/"`) ไม่ใช่ `content_type` เพราะ WCA rule รองรับ filter แค่ `url`/`event` · เป็น seed ของ Lookalike 1% ที่ติดเงื่อนไข pool < 100 คน

### 3.2 แคมเปญ — 🔴 เปิดยิงแล้วตั้งแต่ 2026-07-25 (มีค่าใช้จ่ายจริง)

| สิ่งที่สร้าง | ID | สถานะ |
|---|---|---|
| Campaign `[MR]_Traffic_CaseGame` (OUTCOME_TRAFFIC) | `52588558588397` | **ACTIVE** |
| Ad set `CG1 Broad — TH 20-35` (LPV, **฿150/วัน**, ABO) | `52588558665397` | **ACTIVE** |
| Ad **`★ CG Feed A — โทนมืด`** | `52588606335197` | **ACTIVE** |
| Ad **`★ CG Feed B — โทนสดใส`** | `52588606394397` | **ACTIVE** |
| Ad `★ CG Story A — โทนมืด 9:16` | `52588606403797` | PAUSED |
| Ad `★ CG Story B — โทนสดใส 9:16` | `52588606431197` | PAUSED |
| Ad `CG สำรอง — ภาพแคปจากเกม (Direct)` | `52588583712197` | PAUSED |
| Ad `CG สำรอง — ภาพแคปจากเกม (Hub)` | `52588583728597` | PAUSED |

**เปิดแค่คู่ feed ก่อน** — ทั้งสองยิงเข้า Direct (เคส torsion) เหมือนกันหมด ต่างกันแค่**สไตล์ภาพ** (มืด vs สดใส) แยกกันด้วย `utm_content` = `feed_a_dark` / `feed_b_bright` → เทียบได้ตรงๆ ว่าสไตล์ไหนได้ `casegame_first_decision` ต่อบาทถูกกว่า โดยไม่มีตัวแปรอื่นปน

เหตุที่ยังไม่เปิด Story: ฿150/วันกับ 4 creative พร้อมกันบางเกินไป แต่ละตัวจะได้ข้อมูลน้อยจนสรุปไม่ได้ · เปิดทีละคู่แล้วค่อยขยาย

**ยังไม่เปิด Hub** — Direct ส่งคนไปที่เคสเขียนมือซึ่งเป็นเคสคุณภาพดีที่สุดที่มี และ copy ตรงกับเคสนั้นพอดี ทางขยายคือทำ Direct เพิ่มยิงเข้าคนละเคส ไม่ใช่ไปเปิด Hub — จะได้รู้ด้วยว่าเคสไหนขายดี

⚠️ **ตอนนี้ optimize บน Landing Page View ไปก่อน** เพราะ Custom Conversion ยังไม่ได้สร้าง (§3.1) ใช้ได้สำหรับรอบทดสอบเพราะเราวัด funnel จากระบบตัวเองผ่าน `utm_content` อยู่แล้ว แต่ถ้าอยากให้อัลกอริทึมไปหาคนที่ "เล่นจริง" ต้องสร้าง Custom Conversion แล้วสลับ optimization goal

⚠️ **ไฟล์ `casegame-kv2-4x5-mascot.png` และ `casegame-kv2-9x16-ambulance.png` ยังไม่ได้ทำเป็น ad** — เก็บไว้เป็นรอบสอง (ตัว ambulance มีคำว่า "ของรางวัลพิเศษ" ที่ไม่ตรงกับระบบจริง ดูหมายเหตุใน §2.6)

> **ad รุ่นเก่าปลดระวางแล้ว 4 ตัว** ขึ้นต้นชื่อด้วย `zz เลิกใช้` / `(เลิกใช้ …)` ทั้งหมด PAUSED — `52588558807597`, `52588558810397` (ไม่มี autostart) และ `52588565382197`, `52588565384197` (ภาพเก่า)
>
> creative **แก้ในที่ไม่ได้ทั้ง link และรูป** (immutable) ทุกครั้งที่เปลี่ยนปลายทางหรือเปลี่ยนรูปจึงต้องสร้าง creative + ad ใหม่เสมอ นี่คือเหตุผลที่มี ad รุ่นเก่าค้างอยู่

### 2.6 Creative

มีสองชุดที่สัดส่วนถูก ใช้ยิงคู่กันเพื่อเทียบได้เลย — **อย่าเลือกด้วยความรู้สึก ให้ดูที่ `casegame_first_decision` ต่อบาท**

**ชุด A — โทนมืด จริงจัง (เข้ากับหน้า landing)**
- `public/ads/casegame-kv-4x5.png` (1122×1402) → Feed
- `public/ads/casegame-kv-9x16.png` (941×1672) → Reels / Story

คำโฆษณาตรวจแล้วตรงกับของจริงทุกข้อ: "100+ เคสผู้ป่วย" (จริงมี **157 เคส** published), เก็บ XP + Badge (มีจริงทั้งคู่), "เล่นฟรี ไม่มีค่าใช้จ่าย" (เล่นได้โดยไม่ต้องล็อกอินด้วยซ้ำ) และไอคอน 4 ขั้นตรงกับ flow จริงของเกม

**ชุด B — โทนสดใส สไตล์เกมมือถือ**
- `casegame-kv2-4x5-mlogo.png` → Feed (โลโก้ M เขียว ตรงกับที่ใช้ที่อื่น — แนะนำเป็นตัวหลักของชุดนี้)
- `casegame-kv2-4x5-mascot.png` → Feed (โลโก้ตัวอักษรชมพู + มาสคอต)
- `casegame-kv2-9x16-cpr.png` → Reels / Story
- `casegame-kv2-9x16-ambulance.png` → Reels / Story

ชุดนี้อาจได้ CTR ดีกว่าเพราะดูเล่นง่าย ไม่กดดัน แต่มีสองข้อที่ต้องยอมรับ: (1) หน้า landing เป็นธีมมืด กดจากโฆษณาสีสดเข้าไปจะรู้สึกสะดุด (2) กลุ่มเป้าหมายคือ นศ.แพทย์ที่เครียดกับการสอบ ภาพการ์ตูนอาจอ่านว่า "ไม่จริงจัง" — เดาไม่ได้ ต้องให้ตัวเลขตอบ

⚠️ **ข้อที่รู้อยู่แล้วว่าไม่ตรง (เจ้าของตัดสินใจใช้ทั้งที่รู้)**
- `casegame-kv2-9x16-ambulance.png` เขียน **"เล่นจบรับ ของรางวัลพิเศษ"** — เกมเคสจบแล้วได้แค่ **XP กับ Badge ไม่มีของรางวัล** ระบบแลกของที่มีจริงคือ Bug Hunter (แจ้งข้อสอบผิด → สะสมแต้ม → แลกวันสมาชิก, `app/api/mcq/redeem-points`) ซึ่งคนละเรื่องกัน · ถ้าวันหลังโดน Meta ตีเรื่อง misleading claim หรือมีคนทัก ให้ดูตรงนี้
- ทั้งสองใบ 9:16 เป็นภาพ **CPR / AED / รถพยาบาล** ซึ่งเป็นธีมปฐมพยาบาล ไม่ใช่ Long Case ในวอร์ดตามที่ปลายทางจริงเป็น — ใบ 4:5 ทั้งสองตรงธีมกว่า (คนไข้นอนเตียง + monitor + การ์ด CASE)
- mockup ในภาพโชว์เหรียญ/เพชร ซึ่งเกมไม่มีระบบนั้น
- โลโก้ทั้งชุดมี 3 แบบไม่ตรงกัน — ถ้าจะยิงยาวควรเลือกแบบเดียว

**ชุดสำรอง — แคปจากตัวเกมจริง**
`casegame-ad-4x5.png` (1080×1350) และ `casegame-ad-9x16.png` (1080×1920) — ภาพในกรอบมือถือแคปจาก `/sim/lc-testicular-torsion-01` โทนสีชุดเดียวกับ `components/sim/sim.css` ใช้เป็นตัวเทียบกับ key visual ได้

ของเดิมที่ยกมาจากแคมเปญ Free Trial เป็นแบนเนอร์แบรนด์ 1200×630 (สัดส่วน OG image) ซึ่งพูดเรื่องคลังข้อสอบ ไม่เกี่ยวกับเกม และลง Reels/Story ไม่ได้ — เลิกใช้แล้ว

⚠️ **tool อัปโหลดรูปของ MCP ยังไม่เปิดใช้กับ ad account นี้** — ทางที่ใช้ได้คือส่ง `image_url` ตอนสร้าง creative แล้วให้ Meta ไปดึงเอง จึงต้อง commit รูปเข้า `public/ads/` และ deploy ขึ้น production ก่อนเสมอ (URL ต้องสาธารณะจริง preview URL ของ Vercel ใช้ได้แต่ไม่ถาวร)

**ข้อควรระวังเรื่องสัดส่วน:** อย่าเอาภาพ 9:16 ไปลง feed ตรงๆ Meta จะ crop กลางภาพให้อัตโนมัติ ซึ่งกินทั้งโลโก้ด้านบนและปุ่ม CTA ด้านล่างหายไป — ต้องมีคู่ 4:5 แยกเสมอ

**QR code บนภาพไม่มีประโยชน์ในโฆษณาบนมือถือ** (คนดูอยู่บนจอเดียวกัน สแกนตัวเองไม่ได้) — ไม่ได้เสียหาย แต่ถ้าทำรอบหน้าเอาพื้นที่ตรงนั้นไปใส่อย่างอื่นคุ้มกว่า

ปิด Advantage+ Audience (`advantage_audience: 0`) ให้อายุ 20–35 เป็นช่วงตายตัว ตามกฎไทยห้ามยิงต่ำกว่า 20
ต้องวิ่ง**คู่กับ** `[MR]_Traffic_Browse` (`52580729960597` — เดิมชื่อ `_FreeTrial` · **ACTIVE อยู่จริง** ใช้ไป ฿849 ใน 7 วันแรก) เพื่อเทียบ **ไม่ใช่ไปแทนที่**

**ทุกปลายทางของโฆษณาต้องมี `&start=1`** — เกมจะข้ามจอ title แล้วเริ่มเล่นทันทีที่หน้าโหลด คนกดโฆษณาเข้ามาจึงได้ engage ทันทีโดยไม่ต้องกดอะไรอีก (ผู้ใช้ที่เข้ามาเองยังเจอจอ title ตามปกติ เพราะจอนั้นเป็นที่เลือกระดับความยาก)

**ทดสอบปลายทาง 2 แบบด้วย ad 2 ตัวใน ad set เดียว:**
- ก. Hub → `/casegame?...&utm_content=hub&start=1` — เห็นว่ามีหลายเคส แต่เพิ่ม 1 คลิก (หน้ารวมส่ง `start` ต่อให้เอง)
- ข. Direct → `/sim/lc-testicular-torsion-01?...&utm_content=direct&start=1` — เข้าเกมทันที 0 คลิก

วัดด้วย `casegame_first_decision / LPV` ของแต่ละ ad — ไม่ใช่ `casegame_start` ซึ่งตอนนี้เท่ากับ LPV ไปแล้ว

**เมื่อ Custom Conversion สะสมได้ ~50/สัปดาห์** → เปลี่ยนแคมเปญเป็น Conversions optimize บน event นั้น (นี่คือเป้าหมายจริงของทั้งแผน)

จากนั้นป้อน `[MR]_Retarget` ด้วย audience คนเล่นเกม (exclude คนสมัคร/จ่ายแล้ว)

### 3.3 Creative

ต่อยอด "มุม 2 — Long Case AI" ใน `docs/ads-creative-copy-2026H2.md` แต่เปลี่ยน CTA เป็น **"ลองเล่นเคสฟรี ไม่ต้องสมัคร"** — เป็นข้อเสนอที่ friction ต่ำกว่าทุกตัวที่เคยยิง

### 3.4 Content depth (ทำคู่ขนาน)

เคส built-in มีแค่ 3 เคส ถ้าคนเล่นจบแล้วไม่มีอะไรต่อ retention = 0 และการยิงแอดจะกลายเป็นซื้อคนมาเล่นฟรีเฉยๆ
ใช้ `npm run gen:casegames` (`scripts/generate-longcase-games.ts`) → ตั้งเป้ามีเคส published **≥ 10 เคส** ก่อนเปิดงบเต็ม

✅ **ผ่านแล้ว** — นับจาก DB 2026-07-25: `sim_scenarios` 157 เคส · `long_cases` 107 · `exams` (MEQ) 56 · `mcq_questions` 8,106 ข้อ
(โฆษณาที่เขียน "6,700+ ข้อ" จึงยังต่ำกว่าความจริง = ปลอดภัย ไม่ต้องแก้)

### 3.5 แคมเปญ Browse — แทนที่ FreeTrial (2026-07-25)

`[MR]_Traffic_FreeTrial` เดิมพาคนไป `/register` ด้วยปุ่ม **SIGN_UP** และ ad set optimize บน `OFFSITE_CONVERSIONS` = สั่งให้ Meta ตามหาคนกรอกฟอร์มโดยเฉพาะ ซึ่งขัดกับทิศทางใหม่เต็มๆ จึงแปลงทั้งชุด

| ระดับ | เดิม | ใหม่ |
|---|---|---|
| Campaign `52580729960597` | `[MR]_Traffic_FreeTrial` | `[MR]_Traffic_Browse` |
| Ad set `52580833488197` | `[MR]_Traffic_FreeTrial_Broad` · `OFFSITE_CONVERSIONS` | `[MR]_Browse_Broad` · **`LANDING_PAGE_VIEWS`** · ฿120/วัน |

**ad ใหม่ 4 ตัว — ACTIVE** (copy + รูปเดิมทุกอย่าง เปลี่ยนแค่ปลายทางกับปุ่ม → เทียบกับของเดิมได้ตรง ๆ) ปุ่มทั้งหมดเป็น `LEARN_MORE`

| Ad | ID | Creative | ปลายทาง |
|---|---|---|---|
| ★ Browse — คลังข้อสอบ 6,700+ | `52588820836797` | `1486903913194629` | `/nl/practice?…&utm_content=mcq_bank` |
| ★ Browse — อ่านที่ไหนก็ได้ | `52588820849197` | `1589826949414426` | `/nl/practice?…&utm_content=anywhere` |
| ★ Browse — ครบทุกบท NL/MEQ | `52588820868397` | `2201444814030696` | `/exams?…&utm_content=meq_all` |
| ★ Browse — Long Case AI | `52588820916797` | `1364546772470456` | `/longcase?…&utm_content=longcase_ai` |

ทุกใบต่อ `?utm_source=fb&utm_medium=paid&utm_campaign=browse`

**ad เก่า 4 ตัว PAUSED แล้ว** ขึ้นต้นชื่อ `zz เลิกใช้ — … (พาไป /register)`: `52586206390597`, `52586568651197`, `52586568647997`, `52580833960797`

> เหตุผลที่แต่ละใบลงคนละหน้า = **message match** ใบไหนอวดอะไรก็พาไปดูอันนั้น คนกดเข้ามาแล้วเจอสิ่งที่โฆษณาบอกทันที ไม่ต้องหา
> `/nl/practice` ได้สองใบเพราะเป็นหน้าเดียวที่ **โหลดปุ๊บได้ทำข้อสอบทันทีโดยไม่ต้องล็อกอิน** (ฟรี 5 ข้อ) — engagement เร็วที่สุดในบรรดาหน้าที่มี

---

## 4. เกณฑ์ go/no-go

แก้ทั้งตาราง 2026-07-25 — เกณฑ์เดิม "ให้อีเมล / เล่นจบ > 15%" **ใช้ไม่ได้แล้ว** เพราะไม่มีฟอร์มให้กรอก ตัวชี้ขาดใหม่ต้องเป็นสิ่งที่วัดได้โดยไม่ต้องขออะไรจากผู้ใช้

| ขั้น | ตัววัด | เกณฑ์ผ่าน |
|---|---|---|
| เปิดงบได้ | Custom Conversion สร้างแล้ว + Meta Test Events ผ่าน (§3.1 สองข้อที่ยังค้าง) | ครบทุกข้อ |
| เกมน่าเล่นจริง | `casegame_first_decision` / `casegame_start` | > 70% |
| เกมจบได้จริง | `casegame_complete` / `casegame_start` | > 40% |
| **ตัวชี้ขาด** | `casegame_cta_click` / `casegame_cta_view` — เล่นจบแล้วเดินต่อเข้าเว็บ | **> 20%** |
| ต้นทุน | cost / `casegame_first_decision` | < ฿10 |
| ปลายทาง | signup / คนเข้าเว็บทั้งหมด (`signup_submit` เทียบ LPV) | ตั้ง baseline 2 สัปดาห์แรก แล้วค่อยกำหนดเกณฑ์ |

**ถ้า cta_click rate < 20% หลังเก็บ 2 สัปดาห์ → หยุด อย่าเพิ่มงบ** — แปลว่าคนเล่นเกมสนุกแต่ไม่สนใจของที่เราขาย ปัญหาอยู่ที่ความเชื่อมโยงระหว่างเกมกับสินค้า ไม่ใช่ที่ปริมาณ traffic

⚠️ **อย่าตัดสินแคมเปญนี้ที่ CPC หรือจำนวนคนเล่น** — เกมดึงคนได้ถูกกว่าเสมอ CPC จะสวยขึ้นแน่นอน แต่ CAC ต่อคนจ่ายจริงอาจไม่ดีขึ้นเลย

⚠️ **จุดอ่อนที่ยอมรับไปแล้วของทิศทางใหม่:** เมื่อไม่เก็บอีเมล เราจะ**ไม่มีวิธีติดตามคนที่เข้ามาแล้วยังไม่ตัดสินใจ**เลยนอกจาก retarget การเปิด `[MR]_Retarget` จึงไม่ใช่ของแถม แต่เป็นส่วนที่ขาดไม่ได้ของแผนนี้ — ถ้าไม่เปิด งบที่จ่ายไปกับคนที่มาครั้งเดียวแล้วไม่กลับมาจะสูญเปล่า 100%

---

## 5. ปฏิทิน

รอบสอบถัดไป: **NL-1 รอบ 2/2569 = 2026-10-10** และ **NL-2 รอบ 4/2569 = 2026-10-11** (`lib/exam-dates.ts`)
หน้าต่างอัดงบ 6–8 สัปดาห์ก่อนสอบ = **กลาง ส.ค. → 10 ต.ค.**

| ช่วง | งาน |
|---|---|
| ทันที | §3.1 ทั้งหมด (Test Events → Custom Conversion → Custom Audience) |
| ~สัปดาห์ที่ 2 | เปิด `[MR]_Traffic_CaseGame` งบเล็ก · §3.4 เพิ่มเคสคู่ขนาน |
| กลาง ส.ค. – 10 ต.ค. | หน้าต่างอัดงบ · scale เฉพาะ ad set ที่ผ่านเกณฑ์ §4 |

---

## 6. ความเสี่ยง

- **คนเล่นเกม ≠ คนจ่ายเงิน** — ความเสี่ยงจริง แต่ต่ำกว่าปกติเพราะกลุ่มเป้าหมายแคบมาก (นศ.แพทย์ไทย) คนนอกวงไม่คลิกอยู่แล้ว คุมด้วยเกณฑ์ §4
- **เคสน้อย** — §3.4 ต้องเดินคู่ขนาน ไม่งั้น retention พัง
- **Custom Conversion / Lookalike สร้างผ่าน MCP ไม่ได้** — ต้องทำในหน้า Ads Manager เอง (เหมือนกรณี Lookalike ที่ `ads-campaign-setup-2026H2.md` ระบุไว้) เผื่อเวลาให้เจ้าของบัญชี
